_schema-version: "3.2"
ID: comliptonpricevalidation
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comliptonpricevalidation-destination-content
  type: com.sap.application.content
  requires:
  - name: comliptonpricevalidation-destination-service
    parameters:
      content-target: true
  - name: comliptonpricevalidation-repo-host
    parameters:
      service-key:
        name: comliptonpricevalidation-repo-host-key
  - name: comliptonpricevalidation-uaa
    parameters:
      service-key:
        name: comliptonpricevalidation-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comliptonpricevalidation_html_repo_host
          ServiceInstanceName: comliptonpricevalidation-html5-service
          ServiceKeyName: comliptonpricevalidation-repo-host-key
          sap.cloud.service: comliptonpricevalidation
        - Authentication: OAuth2UserTokenExchange
          Name: comliptonpricevalidation_uaa
          ServiceInstanceName: comliptonpricevalidation-xsuaa-service
          ServiceKeyName: comliptonpricevalidation-uaa-key
          sap.cloud.service: comliptonpricevalidation
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comliptonpricevalidation-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comliptonpricevalidation-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comliptonpricevalidation.zip
      name: comliptonpricevalidation
      target-path: resources/
- name: comliptonpricevalidation
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comliptonpricevalidation-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comliptonpricevalidation-destination-service
    service-plan: lite
- name: comliptonpricevalidation-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comliptonpricevalidation-xsuaa-service
    service-plan: application
- name: comliptonpricevalidation-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comliptonpricevalidation-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
